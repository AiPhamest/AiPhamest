<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.AiPhamest">

    <!-- 🔐 Required permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- <<< FIX: Permission needed to restore alarms after a reboot >>> -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 📸 Declare that the app uses a camera (optional but recommended) -->
    <uses-feature android:name="android.hardware.camera.any" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OneFileApp">

        <activity
            android:name="com.example.AiPhamest.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.OneFileApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Dose alarm/notification receiver -->
        <receiver
            android:name="com.example.AiPhamest.data.DoseAlertReceiver"
            android:exported="false"
            android:enabled="true" />
        <receiver
            android:name="com.example.AiPhamest.data.TakeDoseReceiver"
            android:exported="false"
            android:enabled="true" />
        <receiver
            android:name="com.example.AiPhamest.data.SnoozeReceiver"
            android:exported="false"
            android:enabled="true" />

        <!-- <<< FIX: Add the BootReceiver so alarms can be restored on reboot >>> -->
        <receiver
            android:name="com.example.AiPhamest.data.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>